name: Web Tests

on:
    push:
        branches: [main, develop]
        paths:
            - "apps/web/**"
            - "packages/**"
            - ".github/workflows/web-test.yml"
    pull_request:
        branches: [main, develop]
        paths:
            - "apps/web/**"
            - "packages/**"
            - ".github/workflows/web-test.yml"

jobs:
    e2e-tests:
        name: Playwright E2E Tests
        runs-on: ubuntu-latest

        defaults:
            run:
                working-directory: .

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Set up Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: "20"
                  cache: "npm"

            - name: Install dependencies
              run: npm ci

            - name: Install Playwright browsers
              run: npx playwright install --with-deps
              working-directory: ./apps/web

            - name: Run Playwright e2e tests
              run: npm run test:e2e --workspace=apps/web
              env:
                  NEXT_PUBLIC_API_URL: http://localhost:8000
                  NEXT_PUBLIC_GRAPHQL_URL: http://localhost:8000/graphql
                  NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.TEST_SUPABASE_URL }}
                  NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.TEST_SUPABASE_ANON_KEY }}
                  NEXT_PUBLIC_SUPABASE_PROJECT_ID: ${{ secrets.TEST_SUPABASE_PROJECT_ID }}
                  SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.TEST_SUPABASE_SERVICE_ROLE_KEY }}

            - name: Upload Playwright report
              if: failure()
              uses: actions/upload-artifact@v4
              with:
                  name: playwright-report
                  path: apps/web/playwright-report/
                  retention-days: 7

            - name: E2E tests failed
              if: failure()
              run: |
                  echo "❌ Playwright e2e tests failed!"
                  echo "Run the following command locally to debug:"
                  echo "  npm run test:e2e --workspace=apps/web"
                  exit 1

    summary:
        name: Web Test Summary
        runs-on: ubuntu-latest
        needs: [e2e-tests]
        if: always()

        steps:
            - name: Check test status
              run: |
                  if [ "${{ needs.e2e-tests.result }}" == "failure" ]; then
                    echo "::error::E2E tests failed. Please review the logs above."
                    exit 1
                  else
                    echo "✅ All web tests passed!"
                  fi
